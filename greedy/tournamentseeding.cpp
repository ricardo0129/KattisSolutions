#include <bits/stdc++.h>
using namespace std;
#define ll long long 
#define vi vector<int>
#define vll vector<ll>
#define pii pair<int,int>


ll n,k;
vll A;
int ans = 0;

solve(int i, int l
int main()
{
    cin>>n>>k;
    A.resize((1<<n));
    for(int i=0;i<(1<<n);i++){
        cin>>A[i];
    }
    sort(A.begin(),A.end());


    
    return 0;
}
